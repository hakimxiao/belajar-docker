# Container stats
    # digunakan untuk melihat statistik container seperti memory, cpu, network, dan lain-lain 
    # (tidak hanya container yang sedang berjalan, namun juga container yang sudah mati) jadi ini digunakan untuk melihat statistik
docker container stats